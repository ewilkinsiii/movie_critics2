<%= form_for([@movie, @trailer]) do |f| %>
  <% if @trailer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tailer.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @trailer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title%>
  </div>
  <div class="field">
    <%= f.label :embed %><br>
    <%= f.text_field :embed%>
  </div>
  <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>